import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Ana renk
    },
    secondary: {
      main: '#ff9800', // İkincil renk
    },
  },
});

export default theme;
